services:
  al2:
    build:
      context: .
      dockerfile: ./docker/al2/Dockerfile
    volumes:
      - ./data:/data
    tty: true
  al2023:
    build:
      context: .
      dockerfile: ./docker/al2023/Dockerfile
    tty: true
    volumes:
      - ./data:/data
